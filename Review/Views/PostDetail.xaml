<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Review.Views.PostDetail" >
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand" Padding="150">
            <Label Text="{Binding id ,StringFormat='ID is &quot;{0}&quot;'}" FontSize="Title" HorizontalTextAlignment="Center"/>
            <Label Text="{Binding title}" HorizontalTextAlignment="Center" FontSize="Body"/>
            <Label Text="{Binding body}" HorizontalTextAlignment="Center" FontSize="Caption"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>